# Frontend env. DEV defaults below target the local Supabase CLI stack (`make supabase-up`).
# The anon key is RLS-protected and safe to ship. These are the PUBLIC local demo values
# (localhost-only, not secrets). For PRODUCTION, set VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY
# in the Vercel project env — never commit prod keys. See DEPLOYMENT.md / SECURITY.md.
VITE_SUPABASE_URL=http://127.0.0.1:54321
# Local demo anon key (re-fetch with: supabase status -o env | grep ANON_KEY):
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
